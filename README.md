# Байкал Север

Адаптивный туристический лендинг об экскурсиях и путешествиях по Байкалу.

## Запуск

Откройте `index.html` в браузере. Дополнительная сборка не требуется.

Для локального HTTP-сервера:

```bash
node dev-server.js
```

После запуска сайт доступен по адресу `http://localhost:4173`.
